# Packaging

Use the packaging script to create distributable archives.

## Command
```powershell
./scripts/package.ps1
```

## Output
- `dist/generator-source-v<version>.zip`
- `dist/generator-macos-universal-v<version>.tar.gz` (on macOS only)

## Metadata
See `package/generator.package.json`.
